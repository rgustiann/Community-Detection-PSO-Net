[tool:pytest]
testpaths = .
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*
addopts = 
    --cov=pso_algorithm
    --cov=utils
    --cov=visualization
    --cov-report=html:htmlcov
    --cov-report=term-missing
    --verbose
